# URL base del backend (sin slash al final)
# Web / iOS / dispositivo físico en la misma red:
EXPO_PUBLIC_API_URL=http://127.0.0.1:<PORT>

# Emulador Android (mapea al localhost del PC):
# EXPO_PUBLIC_API_URL=http://10.0.2.2:<PORT>
